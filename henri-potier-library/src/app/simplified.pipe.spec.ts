import { SimplifiedPipe } from './simplified.pipe';

describe('SimplifiedPipe', () => {
  it('create an instance', () => {
    const pipe = new SimplifiedPipe();
    expect(pipe).toBeTruthy();
  });
});
